extract = 'total_dyn_inst: (\d+)'
benchmarks = '../benchmarks-core/*.bril'

[runs.baseline]
pipeline = [
    "bril2json",
    "brili -p {args}"
]

[runs.lvndce]
pipeline = [
    "bril2json",
    "python ../cfg.py", 
    "python ../dce.py", 
    "brili -p {args}"
]